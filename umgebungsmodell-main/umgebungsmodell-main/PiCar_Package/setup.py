from setuptools import setup, find_packages
requirements = []
setup(
    name="virtual_picar",
    version="1.0.0",
    description="PiCar library",
    packages=find_packages(),
    install_requires=requirements
)
